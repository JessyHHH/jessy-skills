# Plan: 修复 Code Review 5 个问题

**Created**: 2026-05-14  
**Scope**: 工作流 code review 发现，2 MEDIUM + 3 LOW

---

## Fixes

### 1. [MEDIUM] Cron job 幂等性 (SKILL.md:331-338)

改为：创建前先 list → 存在则 update，不存在才 create。

```
cronjob(action='list') → if 'hermes-phase7-memory-cron' not found:
  cronjob(action='create', ...)
```

### 2. [MEDIUM] README Phase 7 行对齐 (README.md:32)

缩短为：`Phase 7 → Retro + Learn + Memory Cron (7.1/7.2/7.3)`

### 3. [LOW] category 尾斜杠 (SKILL.md:441)

`project/memory-backup/` → `project/memory-backup`

### 4. [LOW] 触发词中英混合 (SKILL.md:128-129)

压缩算法只生成中文触发词，Phase 0.5 也只匹配中文。去掉 `→ load skill <name>` 以免误导。

### 5. [LOW] 内存使用率读取描述 (SKILL.md:347)

`(injected by cron at each tick)` → 加具体说明：`（检查 conversation context 中 MEMORY 行的使用率百分比，如 96%）`

---

## Files

- `skills/project-workflow/SKILL.md` — #1, #3, #4, #5
- `README.md` — #2

## Verification

- git diff 确认每个改动点
- Phase 数不变（10）
