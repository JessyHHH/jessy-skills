interface:
  display_name: "Project Workflow Codex"
  short_description: "Clarify, contract, and audit Claude execution"
  default_prompt: "Use $project-workflow-codex to ask one-at-a-time clarification questions, generate a Cross-Agent Plan Contract, route skills, and audit Claude Code execution."

policy:
  allow_implicit_invocation: true
