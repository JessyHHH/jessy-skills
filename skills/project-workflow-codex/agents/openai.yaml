interface:
  display_name: "Project Workflow Codex"
  short_description: "Plan contracts and audit Claude execution"
  default_prompt: "Use $project-workflow-codex to generate a Cross-Agent Plan Contract, route skills, and audit Claude Code execution."

policy:
  allow_implicit_invocation: true
