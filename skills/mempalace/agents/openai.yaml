interface:
  display_name: "MemPalace"
  short_description: "Save and search local AI memory"
  default_prompt: "Use $mempalace to save this conversation context into local memory."
dependencies:
  tools:
    - type: "mcp"
      value: "mempalace"
      description: "Local MemPalace MCP server for persistent memory"
      transport: "stdio"
